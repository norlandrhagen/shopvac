## Get top level size of prefixes from a bucket



#### cli usage:

```python

uv run cloud-dir-size \
  --bucket-url gs://<ADD-ME> \
  --min-size-gb 5 \
  --send-slack \
  --slack-webhook-url "<ADDME>"
  ```
